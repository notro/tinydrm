config LCDREG
	tristate
	depends on GPIOLIB || COMPILE_TEST
	default n

config LCDREG_I2C
	tristate
	depends on I2C
	select LCDREG
	default n

config LCDREG_SPI
	tristate
	depends on SPI
	select LCDREG
	default n
